% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/broadcast.R
\name{array_pairwise}
\alias{array_pairwise}
\title{Apply a function to each pair of elements in two arrays, broadcasting
  them to a common dimension first}
\usage{
array_pairwise(mat1, mat2, f, ...)
}
\arguments{
\item{mat1}{A matrix}

\item{mat2}{A matrix}

\item{f}{A function that takes two arguments and returns a single value.}

\item{...}{Additional arguments to be passed to the function `f`.}
}
\value{
An array containing the results of applying `f` to each pair of
  elements in `mat1` and `mat2`
}
\description{
Applies a function to each pair of elements in two arrays,
  broadcasting them to a common dimension first.
}
\examples{
m1 = array(1:4, dim = c(2, 2))
m2 = array(5:8, dim = c(4, 1))
array_pairwise(m1, m2, function(x, y) x^y)
}
